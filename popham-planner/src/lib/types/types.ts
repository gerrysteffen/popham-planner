export type Recipe = {
  name: string;
  id: string;
  image_url: string;
}

export type Restaurant = {
  name: string;
  id: string;
  image_url: string;
}

export type PreviewCubeData = {
  name: string;
  id: string;
  image_url: string;
}